# @package _global_
data:
  batch_size: 2
  num_proc: 0

optimizer:
  lr: 0.01
  weight_decay: 0


defaults:
  - override /data: news_lm
  - override /scheduler: exponential
  - override /optimizer: adamW
  - override /model: lm_model

max_epochs: 12