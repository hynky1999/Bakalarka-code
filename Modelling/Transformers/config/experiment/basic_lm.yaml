# @package _global_
batch_size: 12

optimizer:
  lr: 2e-5
  betas: [0.9, 0.98]
  weight_decay: 0

defaults:
  - override /data: news_lm
  - override /scheduler: linear_warmup
  - override /optimizer: adamW
  - override /model: robeczech_lm

max_epochs: 10
